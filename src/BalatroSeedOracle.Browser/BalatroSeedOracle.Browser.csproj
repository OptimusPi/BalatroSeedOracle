<Project Sdk="Microsoft.NET.Sdk.WebAssembly">
  <PropertyGroup>
    <TargetFramework>net10.0-browser</TargetFramework>
    <OutputType>Exe</OutputType>
    <AssemblyName>BalatroSeedOracle.Browser</AssemblyName>
    <!-- WASM Optimization Settings -->
    <WasmEnableSIMD>true</WasmEnableSIMD>
    <WasmEnableThreads>false</WasmEnableThreads>
    <PublishTrimmed>false</PublishTrimmed>
    <WasmEnableHotReload Condition="'$(Configuration)'=='Release'">false</WasmEnableHotReload>
    <!-- Publish to Motely API wwwroot -->
    <PublishDir>$(MSBuildThisFileDirectory)..\..\external\Motely\Motely.API\wwwroot\BSO\_publish\</PublishDir>
  </PropertyGroup>
  
  <Target Name="DeployWasmOutput" AfterTargets="Publish">
    <ItemGroup>
      <!-- Copy all files recursively, including .dat files -->
      <WasmFiles Include="$(PublishDir)wwwroot\**\*" />
    </ItemGroup>
    <Message Text="Copying $(WasmFiles.Count) files from $(PublishDir)wwwroot to BSO folder" Importance="high" />
    <!-- Copy all files preserving directory structure -->
    <Copy SourceFiles="@(WasmFiles)" 
          DestinationFolder="$(MSBuildThisFileDirectory)..\..\external\Motely\Motely.API\wwwroot\BSO\%(RecursiveDir)" 
          SkipUnchangedFiles="false" />
    <Message Text="Deployment complete. Files copied to: $(MSBuildThisFileDirectory)..\..\external\Motely\Motely.API\wwwroot\BSO\" Importance="high" />
    <RemoveDir Directories="$(PublishDir)" />
  </Target>
  <PropertyGroup Condition="'$(EnableWasmThreads)'=='true'">
    <WasmEnableThreads>true</WasmEnableThreads>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Avalonia.Browser" />
    <PackageReference Include="SkiaSharp.NativeAssets.WebAssembly" />
  </ItemGroup>


  <ItemGroup>
    <ProjectReference Include="..\BalatroSeedOracle\BalatroSeedOracle.csproj" />
  </ItemGroup>

  <!-- Shared property for Motely BSO destination - use source wwwroot -->
  <PropertyGroup>
    <MotelyBSODir>$(MSBuildThisFileDirectory)..\..\external\Motely\Motely.API\wwwroot\BSO\</MotelyBSODir>
  </PropertyGroup>
</Project>
