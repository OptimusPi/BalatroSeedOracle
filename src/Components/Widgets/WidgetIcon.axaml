<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="BalatroSeedOracle.Components.Widgets.WidgetIcon">
    
    <!-- Widget icon display with fallback -->
    <Border Background="Transparent"
            Width="60" Height="60"
            CornerRadius="4">
        <TextBlock Name="IconDisplay"
                   Text="{Binding IconText, FallbackValue=📦}"
                   FontSize="32"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>
    </Border>
</UserControl>