# Filter Category Navigation TODO

- [x] Resolve AVLN3000 compile error in `SelectableCategoryButton.axaml` by removing invalid `Classes` setter.
- [x] Align filter category button width with search bar using shared `FilterNavControlWidth` constant.
- [ ] Coordinate follow-up UX review once product owner validates latest sidebar layout.
